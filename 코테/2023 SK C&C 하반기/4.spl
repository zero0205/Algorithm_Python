-- 2019년 1월의 일별 누적 매출액수
SELECT DAY(SCHEDULED_AT) AS "일", SUM(PRICE) 
FROM SCHEDULES
WHERE(SCHEDULED_AT>DATE("2019-01-01") AND SCHEDULED_AT < DATE("2019-02-01"))
GROUP BY DAY(SCHEDULED_AT)
ORDER BY SCHEDULED_AT;